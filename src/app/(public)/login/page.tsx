import { SignInButton } from "@/app/_components/SignButton";
import React from "react";

const LoginPage = () => {
  return (
    <div>
      <h2>Please sign in via Github!</h2>
      <SignInButton />
    </div>
  );
};

export default LoginPage;
